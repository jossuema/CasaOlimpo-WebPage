---
const { thumb_src, title, collection, cta } = Astro.props;

const src = import.meta.env.BASE_URL + 'public/images/' + thumb_src;

console.log(src);

---
    <div class="bg-cover bg-center text-white relative w-full h-full bg-white rounded-lg shadow-lg box-content" style={{ backgroundImage: `url(${src})` }}>
        <img src={src}>
        <div class="bg-black bg-opacity-50 p-6 absolute bottom-0 w-full h-full">
            <h2 class="text-xl font-bold">{title}</h2>
            <p>{collection}</p>

            <a href="#" class="text-white text-sm font-semibold mb-0">Ver productos &#62;</a>
        </div>
    </div>
